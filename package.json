{
  "name": "lambda-job",
  "version": "1.0.0",
  "description": "Use AWS Lambda to run scalable server-side code from a web browser",
  "main": "lambda-job.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "aws",
    "lambda"
  ],
  "author": "Larry Gadea",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.1.4"
  }
}
